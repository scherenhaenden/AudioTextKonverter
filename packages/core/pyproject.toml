[tool.poetry]
name = "magnetar-aion-core"
version = "0.1.0"
description = ""
authors = ["Jules"]
packages = [
    { include = "magnetar_aion" }
]

[tool.poetry.dependencies]
python = ">=3.12"
openai = "^2.8.0"

[tool.poetry.group.dev.dependencies]
pytest = "^9.0.1"
pytest-mock = "^3.15.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
